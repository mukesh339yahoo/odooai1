<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View -->
    <record id="view_field_security_rule_form" model="ir.ui.view">
        <field name="name">field.security.rule.form</field>
        <field name="model">field.security.rule</field>
        <field name="arch" type="xml">
            <form string="Field Security Rule">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger" invisible="active == True"/>
                    <group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="model_id" options="{'no_create': True}"/>
                            <field name="field_id" options="{'no_create': True}" 
                                   context="{'default_model_id': model_id}"/>
                        </group>
                        <group>
                            <field name="rule_type"/>
                            <field name="group_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree View -->
    <record id="view_field_security_rule_tree" model="ir.ui.view">
        <field name="name">field.security.rule.tree</field>
        <field name="model">field.security.rule</field>
        <field name="arch" type="xml">
            <tree string="Field Security Rules">
                <field name="model_id"/>
                <field name="field_id"/>
                <field name="rule_type"/>
                <field name="group_ids" widget="many2many_tags"/>
            </tree>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_field_security_rule_search" model="ir.ui.view">
        <field name="name">field.security.rule.search</field>
        <field name="model">field.security.rule</field>
        <field name="arch" type="xml">
            <search string="Search Rules">
                <field name="model_id"/>
                <field name="field_id"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Model" name="group_by_model" domain="[]" context="{'group_by': 'model_id'}"/>
                    <filter string="Rule Type" name="group_by_rule_type" domain="[]" context="{'group_by': 'rule_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_field_security_rule" model="ir.actions.act_window">
        <field name="name">Field Security Rules</field>
        <field name="res_model">field.security.rule</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first field security rule!
            </p>
            <p>
                Use rules to hide, make read-only, or require specific fields for certain user groups without custom code.
            </p>
        </field>
    </record>

    <!-- Menu Item under Settings > Technical > Security -->
    <menuitem id="menu_field_security_rule"
              name="Field Security Rules"
              parent="base.menu_security"
              action="action_field_security_rule"
              sequence="100"/>

</odoo>
